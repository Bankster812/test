"""Runnable, self-contained worked examples for the IB financial models.

These use the deterministic model classes directly (no neural settling, no
network, no Brain construction) so they run instantly and reproducibly.
"""
