[pytest]
# Anchor rootdir at the repo root and put it on sys.path so `import neuromorphic`
# works under a bare `pytest` invocation (as CI runs it), not just `python -m pytest`.
pythonpath = .
testpaths = tests
