# Core neuromorphic engine
numpy>=1.26.0
scipy>=1.12.0
matplotlib>=3.8.0

# IB domain — Excel read/write/audit
openpyxl>=3.1.0

# IB domain — PDF ingestion (optional, graceful degradation if missing)
pdfplumber>=0.10.0

# IB domain — Web learning (optional)
beautifulsoup4>=4.12.0

# IB domain — YouTube transcript learning (optional)
# yt-dlp>=2024.1.0
# youtube-transcript-api>=0.6.2

# Wholesale domain — Claude LLM client (optional, falls back to heuristics)
# anthropic>=0.25.0

# Wholesale domain — Camoufox stealth browser for Zillow / Realtor / Auction.com
# sourcing. Install: pip install "camoufox[geoip]" && python -m camoufox fetch
# Without it, Scout falls back to HUD / HomePath / CSV import only.
# camoufox[geoip]>=0.4.11
# PyYAML>=6.0           # camoufox needs CLoader from PyYAML 6+
